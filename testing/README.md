This project runs fingerprinting resistance tests using selenium and saves the results to a JSON file.

To set up:

`npm install`

To run tests, point to a browserprivacy.config file:

`node index browserprivacy.config`
